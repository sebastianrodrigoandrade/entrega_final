<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito</title>
</head>
<body>
    <h1>Carrito</h1>
    {{#if cart.length}}
      <ul>
        {{#each cart}}
          <li>{{title}} - ${{price}} - Cantidad: {{quantity}}</li>
        {{/each}}
      </ul>
      <a href="/">Seguir comprando</a>
    {{else}}
      <p>El carrito está vacío</p>
      <a href="/">Seguir comprando</a>
    {{/if}}
</body>
</html>
